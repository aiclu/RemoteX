<UserControl x:Class="_1RM.View.Editor.Forms.SftpFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:_1RM.Controls"
             xmlns:forms="clr-namespace:_1RM.View.Editor.Forms"
             xmlns:s="https://github.com/canton7/Stylet"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance forms:SftpFormViewModel}"
             d:DesignHeight="1450" d:DesignWidth="800">
<UserControl.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="/RemoteX;component/Resources/Theme/FluentForms.xaml"/></ResourceDictionary.MergedDictionaries></ResourceDictionary></UserControl.Resources>
    <StackPanel>

        <TextBlock Style="{StaticResource FluentEditorGroupTextBlockTitle}" Text="{DynamicResource 'Connection'}"></TextBlock>
        <ContentControl s:View.Model="{Binding HostViewModel}"></ContentControl>
        <ContentControl s:View.Model="{Binding CredentialViewModel}"></ContentControl>
        <ContentControl s:View.Model="{Binding AlternativeCredentialListViewModel}"></ContentControl>

        <Grid Style="{StaticResource FluentEditorRowGrid}" Visibility="{Binding New.SelectedRunnerIsInternalRunner, Converter={StaticResource ConverterBool2Visible}}">
            <StackPanel Style="{StaticResource FluentFieldStack}">
                <TextBlock Style="{StaticResource FluentEditorRowGridTitle}" Text="{DynamicResource server_editor_advantage_sftp_startup_path}"></TextBlock>
                <Grid Style="{StaticResource FluentEditorRowGridInput}">
                    <TextBox Text="{Binding New.StartupPath, UpdateSourceTrigger=PropertyChanged}"
                             Tag="e.g. /home/user/Desktop"></TextBox>
                </Grid>
            </StackPanel>
        </Grid>

    </StackPanel>
</UserControl>
